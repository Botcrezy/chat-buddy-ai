

# خطة إصلاح شاملة: البوت مش بيرد + Send API + تحسينات

## تشخيص المشكلة الأساسية
محادثة المستخدم `35889317204169` عندها `is_ai_active: false` (بسبب طلب إسكاليشن سابق). البوت شغال كويس مع المستخدمين التانيين (اختبرت وجاب رد).

## التغييرات المطلوبة

### 1. إعادة تفعيل AI تلقائيا بعد فترة (`whatsapp-webhook/index.ts`)
- لو المحادثة `is_ai_active: false` ولكن آخر رسالة من العميل أقدم من ساعة واحدة، يعيد تفعيل AI تلقائيا
- أو لو العميل بعت رسالة جديدة ومفيش رد من agent خلال 30 دقيقة، يفعل AI

### 2. إصلاح `send-message/index.ts`
- إضافة error handling أفضل
- التأكد من إن الرسالة بتتبعت فعلا للـ Baileys server

### 3. تحسين جودة الردود (`whatsapp-webhook/index.ts`)
- زيادة `max_tokens` من 150 لـ 300 عشان الردود متتقطعش
- تحسين matching بيانات التدريب: لو مفيش keyword match، يرجع أول 5 عناصر عامة بدل مفيش حاجة
- إضافة fallback لما مفيش بيانات تدريب مطابقة

### 4. دعم أفضل للصور والصوت
- لو الرسالة فيها `media_type` والموديل مش بيدعم multimodal، يضيف وصف واضح في الرسالة
- لو صورة: "العميل بعت صورة - اسأله عنها"
- لو صوت: "العميل بعت رسالة صوتية - اسأله يكتبلك"

### 5. إضافة logging أفضل
- log لما `is_ai_active = false` عشان نعرف السبب
- log لنتيجة كل محاولة AI

## الملفات المتأثرة
- `supabase/functions/whatsapp-webhook/index.ts`
- `supabase/functions/send-message/index.ts`

## ملاحظة مهمة
محادثة المستخدم الحالي (`a04d3a02-f00c-4d16-9fcc-a2c895dbd664`) محتاجة تتعمل reset لـ `is_ai_active: true` عبر migration أو من لوحة التحكم.

